{
  "version": "1.0",
  "project": "Proamory",
  "description": "Proamory - Project-specific MCP servers with isolated credentials",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${HOBBY_GITHUB_TOKEN}"
      }
    }
  }
}
